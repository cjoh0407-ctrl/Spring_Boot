## 연습 4.1
***

다음 클래스는 두 개의 `String` 입력을 받아 두 입력 중 더 짧은 단어를 반환하는 단 하나의 메서드만 포함하고 있습니다. 두 단어의 길이가 동일하다면, 둘 중 아무 단어나 반환해도 됩니다.

#### 메서드 `getShortestWord`를 작성하세요.

<div class="hint">
  여기서 객체가 필요하지 않으므로, 메서드는 `static`이어야 합니다.
</div>